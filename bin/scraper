#!/usr/bin/env node

var program = require( "commander" );

program
	.version( "0.0.1" )
	.command( "export", "Export events from riak" )
	.command( "import", "Import events back into riak" )
	.parse( process.argv );